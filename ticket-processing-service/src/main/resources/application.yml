spring:
  application:
    name: ticket-processing-service

  kafka:
    bootstrap-servers: kafka:9092

#  redis:
#    host: redis
#    port: 6379

  data:
    mongodb:
      uri: mongodb+srv://adminColdplay:A0Qmud4BgwVmOsaF@cluster0.w5qkm.mongodb.net/authDB?appName=Cluster0

server:
  port: 8082

eureka:
  instance:
    hostname: ticket-processing-service
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://service-registry:8761/eureka
